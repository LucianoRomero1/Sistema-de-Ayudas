<!DOCTYPE html>
<html >
    <head>
        <meta charset="UTF-8">
	    <meta name="viewport" content="width=device-width, initial-scale=1">
      

        <title>{% block title %}{% endblock %}</title>
        {% block stylesheets %}
            <link rel="icon" href="{{ asset("img/iconoUnraf.png") }}">
            <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">
            <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
            <link href = "{{asset("css/estilos.css")}}" rel = "stylesheet" type = "text/css">
            
            
        {% endblock %}
    </head>
    
    <body>
        {% block body %}
            {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_SUPERADMIN') or is_granted('ROLE_EDITOR') %}

        <nav class="navbar navbar-expand-md navbar-light bg-light">
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target=".dual-collapse2">
                <span class="navbar-toggler-icon"></span> 
            </button>  
            {% if app.request.pathinfo == "/sistema" %}
                    <a class="nav-link" href="{{path('sistema')}}"><img src = "{{asset ('svg/home.svg')}}" alt="Home" width="32"></a>
                {% else %}
                    <a class="nav-link" href="{{path('sistema')}}"><img src = "{{asset ('svg/home_blanco.svg')}}" alt="Home" width="32"></a>
            {% endif %}
            
            <div class="collapse navbar-collapse dual-collapse2 mx-auto order-3 " id="navbarSupportedContent">
                <ul class="navbar-nav mr-auto " style = "text-decoration: none;">
                {%  if is_granted('ROLE_ADMIN') or is_granted('ROLE_SUPERADMIN')%}
                    <li class="nav-item dropdown m-2" id= "navBarMargin">
                        {% if app.request.pathinfo == "/altaPerfiles" or app.request.pathinfo == "/verPerfiles" or "/modificarPerfiles" in app.request.pathinfo or "/asignarCategorias" in app.request.pathinfo %}
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <b id = "seleccionado">Perfiles</b>
                        </a>
                        {% else %}
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Perfiles
                        </a>
                        {% endif %}
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <a id = "navHover" class="dropdown-item" href="{{path('altaPerfiles')}}">Alta</a>
                            <div class="dropdown-divider"></div>
                            <a id = "navHover" class="dropdown-item" href="{{path('verPerfiles')}}">Ver / Modificar</a>
                        </div>
                    </li>
                    <li class="nav-item dropdown m-2">
                        {% if app.request.pathinfo == "/altaCategorias" or app.request.pathinfo == "/verCategorias" or "/modificarCategoria" in app.request.pathinfo %}
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <b id = "seleccionado">Categoria Principal</b>
                        </a>
                        {% else %}
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Categoria Principal
                        </a>
                        {% endif %}
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <a  id = "navHover" class="dropdown-item" href="{{path('altaCategorias')}}">Alta</a>
                            <div class="dropdown-divider"></div>
                            <a  id = "navHover" class="dropdown-item" href="{{path('verCategorias')}}">Ver / Modificar</a>
                        </div>
                    </li>
                    <li class="nav-item dropdown m-2">
                        {% if app.request.pathinfo == "/altaCategoriasSecundarias" or app.request.pathinfo == "/verCategoriasSecundarias" or "/modificarSecundarias" in app.request.pathinfo %}
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <b id = "seleccionado">Categoria Secundaria</b>
                        </a>
                        {% else %}
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Categoria Secundaria
                        </a>
                        {% endif %}
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <a  id = "navHover" class="dropdown-item" href="{{path('altaCategoriasSecundarias')}}">Alta</a>
                            <div class="dropdown-divider"></div>
                            <a  id = "navHover" class="dropdown-item" href="{{path('verCategoriasSecundarias')}}">Ver / Modificar</a>
                        </div>
                    </li>
                    <li class="nav-item dropdown m-2">
                        {% if app.request.pathinfo == "/altaDestino" or app.request.pathinfo == "/verDestino" or "/modificarDestino" in app.request.pathinfo or "/asignarCategoriaDestino" in app.request.pathinfo %}
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <b id = "seleccionado">Destinos</b>
                        </a>
                        {% else %}
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Destinos
                        </a>
                        {% endif %}
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <a  id = "navHover" class="dropdown-item" href="{{path('altaDestino')}}">Alta</a>
                            <div class="dropdown-divider"></div>
                            <a  id = "navHover" class="dropdown-item" href="{{path('verDestino')}}">Ver / Modificar</a>
                        </div>
                    </li>
                    {% endif %}
                    <li class="nav-item dropdown m-2" style = "text-decoration: none;">
                        {% if app.request.pathinfo == "/altaInformacion" or app.request.pathinfo == "/verInformacion" or "/modificarInformacion" in app.request.pathinfo %}
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <b id = "seleccionado">Información</b>
                        </a>
                        {% else %}
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Información
                        </a>
                        {% endif %}
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <a  id = "navHover" class="dropdown-item" href="{{path('altaInformacion')}}">Alta</a>
                            <div class="dropdown-divider"></div>
                            <a  id = "navHover" class="dropdown-item" href="{{path('verInformacion')}}">Ver / Modificar</a>
                        </div>
                    </li>
                    {% if is_granted('ROLE_SUPERADMIN') %}
                    <li class="nav-item dropdown m-2">
                        {% if app.request.pathinfo == "/superadmin/roles" %}
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <b id = "seleccionado">Roles</b>
                        </a>
                        {% else %}
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Roles
                        </a>
                        {% endif %}
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <a class="dropdown-item" href="{{path('roles')}}">Dar Roles</a>                             
                        </div>
                    </li>
                    {% endif %}
                </ul>
            </div>  
            <div class="navbar-collapse collapse w-100 order-3 dual-collapse2">
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item">
                        <a class = "nav-link" href = "{{path('app_logout')}}"><i class="fas fa-sign-out-alt" style = "font-size: 30px; color:#FEB413;"></i></a>
                    </li>
                </ul>
            </div>
               
        </nav>
            {% endif %}
        <div class="container">
            {% for message in app.flashes('info') %}
                <div class="alert alert-success alert-dismissible mt-4 mb-3">
                    <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
                    {{ message }}
                </div>
            {% endfor %}
            {% for message in app.flashes('error') %}
                <div class="alert alert-danger alert-dismissible mt-4 mb-3">
                    <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
                    {{ message }}
                </div>
            {% endfor %}
            {% for message in app.flashes('warning') %}
                <div class="alert alert-warning alert-dismissible mt-4 mb-3">
                    <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
                    {{ message }}
                </div>
            {% endfor %}
        </div> 
        {% endblock %}
        
        <script src="https://code.jquery.com/jquery-3.5.1.min.js" integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=" crossorigin="anonymous"></script>{% block javascripts %}
        <script type="text/javascript" src="//cdnjs.cloudflare.com/ajax/libs/moment.js/2.15.2/moment.min.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ho+j7jyWK8fNQe+A12Hb8AhRq26LrZ/JpcUGGOn+Y7RsweNrtN/tE3MoK7ZeZDyx" crossorigin="anonymous"></script>
        
        
        {# <script src= "{{asset('js/accionesQuery.js')}}"></script>
        <script src= "{{asset('js/validacionesExtras.js')}}"></script> #}
      

        
        <script src="https://use.fontawesome.com/releases/v5.15.2/js/all.js" data-search-pseudo-elements></script>
        


        {% endblock %}
    </body>
</html>
